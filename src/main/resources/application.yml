spring:
  application:
    name: mobilebg

  datasource:
    driverClassName: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/mobilebg?useSSL=false&createDatabaseIfNotExist=true
    username: root
    password: denis

  jpa:
    database-platform: org.hibernate.dialect.MySQL8Dialect
    hibernate:
      ddl-auto: update

